#include <stdio.h>

int main()
{
    // int a;
    // scanf("%d", &a);
    // while (a < 10)
    // {
    //     printf("%d\n", a);
    //     a++;
    // }

    int i = 0;
    while (i <= 20)
    {
        if (i >= 20)
        {
            printf("The Value of i is %d\n", i);
        }
    }
    return 0;
}

// TODO In while loop only true value is executed.