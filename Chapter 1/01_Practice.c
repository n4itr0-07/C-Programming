#include <stdio.h>
int main()
{
    int length, breadth;
    printf("What is the length of reactangle \n");
    scanf("%d", &length),
        printf("What id the Breadth of reactangle \n");
    scanf("%d", &breadth);
    printf("The Area Of Your Reactangle Is : %d" , length * breadth);
    return 0;
}

